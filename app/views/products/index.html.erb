<% provide(:title, title) %>

<ul class="products">
  <% @products.each do |product| %>
    <%= render "shared/product_info", { product: product } %>
    <%= link_to "Add to cart", user_cart_item_path(current_user, product), method: :patch %>
    <br>
  <% end %>
</ul>
